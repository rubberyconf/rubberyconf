meta:
  description: 'toggle about xYZ'
  tags:
    - Foo
default:  
  ttl: 120s
  value: #default value
    data: "{value:0}"
    type: json

configurations:
  - config: 
    id: 1
    rules:
      environment:
        - production
        - staging
      acl:
        - groupUser1 # only apply to this user group 

    rollout: allAtOnce
    value: "{value:1}"

  